name: Release

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'

permissions:
  contents: write
  packages: write

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - goos: linux
            goarch: amd64
            name: bin-notifier-linux-amd64
            cmd: ./cmd/notifier
          - goos: linux
            goarch: arm64
            name: bin-notifier-linux-arm64
            cmd: ./cmd/notifier
          - goos: darwin
            goarch: arm64
            name: bin-notifier-darwin-arm64
            cmd: ./cmd/notifier
          - goos: linux
            goarch: amd64
            name: bin-notifier-api-linux-amd64
            cmd: ./cmd/api
          - goos: linux
            goarch: arm64
            name: bin-notifier-api-linux-arm64
            cmd: ./cmd/api
          - goos: darwin
            goarch: arm64
            name: bin-notifier-api-darwin-arm64
            cmd: ./cmd/api

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.26'
          cache: true

      - name: Build
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
        run: go build -o ${{ matrix.name }} ${{ matrix.cmd }}

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.name }}
          path: ${{ matrix.name }}

  docker:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - dockerfile: Dockerfile
            context: .
            image: ${{ github.repository }}
          - dockerfile: Dockerfile.api
            context: .
            image: ${{ github.repository }}-api
          - dockerfile: mcp/Dockerfile
            context: mcp
            image: ${{ github.repository }}-mcp
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to Container Registry
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ matrix.image }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest

      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=${{ matrix.dockerfile }}
          cache-to: type=gha,mode=max,scope=${{ matrix.dockerfile }}

  release:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v8
        with:
          path: artifacts

      - name: Create zip files
        run: |
          cd artifacts
          for dir in */; do
            name="${dir%/}"
            chmod +x "$name/$name"
            zip -j "${name}.zip" "$name/$name"
          done

      - name: Create Release
        uses: softprops/action-gh-release@v3
        with:
          files: artifacts/*.zip
          generate_release_notes: true
